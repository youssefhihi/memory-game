import { ScoreFormatPipe } from './score-format.pipe';

describe('ScoreFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new ScoreFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
